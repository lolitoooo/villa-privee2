{% extends 'base.html.twig' %}

{% block title %}Réservation confirmée{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="mb-4">
                <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
            </div>
            <h1 class="h2 mb-4">Votre réservation est confirmée !</h1>
            <p class="lead mb-4">
                Merci pour votre réservation. Vous recevrez bientôt un email de confirmation avec tous les détails.
            </p>
            <div class="d-grid gap-3 d-sm-flex justify-content-sm-center">
                <a href="{{ path('app_villa_index') }}" class="btn btn-primary">
                    <i class="bi bi-house-door me-2"></i>Retour à l'accueil
                </a>
                <a href="{{ path('app_user_reservations') }}" class="btn btn-outline-primary">
                    <i class="bi bi-calendar3 me-2"></i>Voir mes réservations
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
